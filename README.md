# Chess-Cplusplus

Used object oriented programming to code up a chess program in C++. 

## Running the Program

The chessmain.cpp file contains a sample game that is played through and displayed using the specified classes. To run the program, run 'make' in the command line which will create an executable titled 'chess' (that can be executed by running './chess'). To clean, run 'make clean'. 
